# Markdown 格式规范模板

本模板定义了所有 Prompt 共享的格式规范，在各个 Prompt 中通过引用本模板来保持一致性。

---

## 一、标题层级规范

| 层级 | 语法 | 用途 |
|------|------|------|
| 一级标题 | `#` | **严禁使用**（由系统自动生成，或仅用于原文标题） |
| 二级标题 | `##` | 主标题（系统添加）或文章主要章节 |
| 三级标题 | `###` | 子标题 |
| 四级标题 | `####` | 更细层级 |

---

## 二、文本格式规范

### 2.1 强调与引用

- **加粗**：关键数据、重要术语用 `**加粗**`
- *斜体*：可选用于次要强调 `*斜体*`
- 引用观点：使用 `> ` 引用块

```markdown
> 这是一段引用的观点或评论
```

### 2.2 列表规范

**有序列表**（步骤、排名等）：
```markdown
1. 第一项
2. 第二项
3. 第三项
```

**无序列表**（并列项目）：
```markdown
- 项目一
- 项目二
- 项目三
```

### 2.3 板块分隔

使用 `---` 分隔不同板块。

---

## 三、技术元素规范

### 3.1 代码块

必须标注语言类型：

````markdown
```python
def hello():
    print("Hello, World!")
```
````

### 3.2 数学公式（LaTeX）

| 类型 | 语法 | 示例 |
|------|------|------|
| 行内公式 | `$公式$` | `$E=mc^2$` |
| 块级公式 | `$$公式$$` | `$$\sum_{i=1}^{n} x_i$$` |

### 3.3 表格

**数据对比 ≥4 项时必须使用表格**：

```markdown
| 指标 | 方案A | 方案B |
|------|-------|-------|
| 性能 | 100ms | 50ms  |
| 成本 | $10   | $15   |
```

---

## 四、图片处理规范

### 4.1 核心规则

1. **原位保留**：所有 `![描述](URL)` 语法必须原样保留
2. **禁止移动**：图片必须保留在原始上下文位置
3. **URL 不变**：不得修改图片 URL

### 4.2 图片过滤规则（提取时）

- ✅ 保留：技术架构图、产品截图、数据图表、流程图
- ❌ 过滤：人物头像、.ico 图标、广告图、追踪像素

---

## 五、JSON 输出规范

### 5.1 基础格式

```json
{
    "title": "标题",
    "content": "Markdown 内容",
    "keywords": ["关键词1", "关键词2"]
}
```

### 5.2 转义规则

| 字符 | 转义 |
|------|------|
| 换行符 | `\n` |
| 双引号 | `\"` |
| 反斜杠 | `\\` |

### 5.3 注意事项

- 确保 JSON 可被 `JSON.parse()` 正确解析
- **严禁**在行末使用单独的 `\\` 作为换行标记

---

## 六、写作风格规范

### 6.1 去 AI 化要求

- **直击核心**：取消过渡废话（如"综上所述"、"总而言之"）
- **精简表述**：每句话都要有信息量
- **专业语调**：使用中性陈述句，避免"极其"、"非常"等形容词
- **数据支撑**：用具体数字代替模糊描述

### 6.2 排除噪音

严禁包含以下内容：
- 高管个人履历
- 合作伙伴公司简介
- 空洞的公司愿景
- 社区贡献者名单
- 页脚版权信息

---

## 七、禁止事项

1. **禁止 HTML 标签**：严禁使用 `<sup>`、`<sub>` 等 HTML 标记
2. **禁止引用标记**：不要添加 `[1]`、`[2]` 等引用标记
3. **禁止废话**：严禁"详情请参考"、"更多信息请访问"等套话
