# AI Trend Publish - 内容模式与工作流指南

本文档总结了当前系统中支持的所有内容模式及其核心处理流程。

## 1. 核心处理流程 (通用)

所有模式都遵循一套标准化的处理管道，旨在确保输出内容的高质量、去重和美观：

1.  **数据抓取 (Scraping)**：根据模式从不同渠道抓取原始内容。
2.  **URL 全局去重**：检查 `UrlRegistry`，跳过已发布过的 URL。
3.  **LLM 预提取 (Pre-extraction)**：
    *   调用大模型从原始网页（包含广告、导航等噪音）中提取**纯净正文**。
    *   自动识别并保存正文中的**核心插图 URL**。
4.  **内容过滤**：过滤掉超过 7 天的过时新闻。
5.  **本地备份**：在 `./data/scraped/` 下保存 `_raw.md` (原始) 和 `_extracted.md` (LLM 提取) 文件。
6.  **深度总结 (Summarization)**：LLM 根据提取后的正文生成具有吸引力的标题、精炼的段落和关键词。
7.  **封面图生成**：根据最终标题生成专业的信息图表风格封面图。
8.  **文章归档**：将最终生成的 Markdown、HTML 和媒体资源保存到 `./data/articles/` 目录。
9.  **微信发布**：通过微信公众号 API 进行发布。

---

## 2. 模式详述

### 模式 A: `TECH_NEWS` (默认模式)
*   **定位**：聚合全球顶级 AI/科技媒体的最新动态。
*   **数据源**：预定义的 FireCrawl 抓取列表（如 AI Business, AI Magazine 等）。
*   **特殊逻辑**：
    *   **内容评分 (Ranking)**：使用大模型对抓取到的数十篇文章进行评分，筛选最有价值的内容。
    *   **AI 插图补充**：如果提取出的正文中没有高质量图片，会自动调用 AI 生成配图。
*   **运行命令**：`deno task test --mode TECH_NEWS`

### 模式 B: `AI_NEWS_SITE`
*   **定位**：针对 `artificialintelligence-news.com` 进行垂直抓取与精细化翻译。
*   **数据源**：固定站点。
*   **特殊逻辑**：
    *   **两步爬取**：先抓取首页获取最新列表，再进入子页面抓取全文。
    *   **精华提取**：LLM 按照“翻译+精华提炼”模式处理，每篇目标字数约 600 字。
    *   **固定输出**：每次固定从最新抓取中输出 5 篇文章。
*   **运行命令**：`deno task test --mode AI_NEWS_SITE`

### 模式 C: `GITHUB_TRENDING`
*   **定位**：分享 GitHub 上当日最热门的开源项目。
*   **数据源**：GitHub Trending 页面。
*   **特殊逻辑**：
    *   **专用去重**：使用 `GitHubProjectRegistry` 记录已处理的项目名。
    *   **专家总结**：LLM 以技术专家的视角总结项目用途、核心技术点和适用场景。
*   **运行命令**：`deno task test --mode GITHUB_TRENDING`

### 模式 D: `SINGLE_URL`
*   **定位**：转载模式。完整翻译单一网页内容并做少量精辟扩充，适合转载优质外文文章。
*   **特点**：
    *   **单篇文章输出**：不添加多篇汇总的引入、整体介绍图，标题层级自动提升（`##` → 主标题，`###` → 小标题）。
    *   **轻量级预提取**：使用专用的轻量 LLM 提取（保留更多原文，只删广告/导航/页脚），而非深度精简。
    *   **完整翻译**：LLM 将提取后的英文内容完整翻译为中文，并做极少量术语解释或背景扩充。
    *   **字数控制**：严格限制在 2000 字以内（可由 `ARTICLE_MAX_LENGTH` 配置）。
    *   **标题格式**：固定为「转载{作者}（作者）」；无法识别作者时为「转载（作者）」。
*   **模型配置**（使用轻量快速模型）：
    *   `SINGLE_URL_LLM_PROVIDER`：默认 `GEMINI`（用于预提取和翻译）
    *   `SINGLE_URL_LLM_MODEL`：默认 `gemini-2.0-flash-exp`
    *   `ARTICLE_MAX_LENGTH`：最大字数，默认 `2000`
*   **流程详解**：
    1. FireCrawl 抓取原始 Markdown
    2. **轻量级 LLM 预提取**：保留完整正文，只删除广告/导航/页脚（使用 `extractContentForSingleUrl`，固定 Gemini Flash）
    3. **完整翻译 + 精辟扩充**：将英文正文翻译为中文，并做极少量扩充（使用 `translateAndLightExpandForRepost`）
    4. 从正文识别作者，设置标题为 `转载{作者}（作者）`
    5. **单篇组装**：不添加 `## 文章标题` 层，内容的 `###` 自动成为文章的小标题
*   **输入**：需要提供 `--url` 参数。
*   **运行命令**：`deno task test --mode SINGLE_URL --url https://example.com/article`

### 模式 E: `TOPIC_SEARCH`
*   **定位**：针对特定主题在全网搜索并生成专题文章。
*   **输入**：需要提供 `--topic` 参数。
*   **运行命令**：`deno task test --mode TOPIC_SEARCH --topic "Sora 视频生成模型"`

---

## 3. 模板系统

系统支持多种微信排版模板，可通过 `.env` 配置 `ARTICLE_TEMPLATE_TYPE` 选择：

| 模板名称 | 配置值 | 特点 | 适用场景 |
|---------|--------|------|---------|
| **默认** | `default` | 简洁实用，信息密度高 | 通用场景 |
| **现代** | `modern` | 清新现代，带目录导航 | 多篇汇总 |
| **科技** | `tech` | 蓝色主题，科技感强 | 技术类文章 |
| **量子位** | `qbit` | 专业媒体风格，深色配色 | 深度报道 |
| **面包** | `mianpro` | 温馨柔和，绿色主题 | 轻松阅读 |
| **极简** | `minimal` | 极简主义，最大化内容 | 单篇精读 ✨ |
| **卡片** | `card` | 渐变卡片，视觉冲击 | 多篇快览 ✨ |
| **学术** | `academic` | 论文风格，衬线字体 | 研究论文 ✨ |
| **未来** | `future` | 科幻感，全息效果 | 前沿科技 ✨ |
| **随机** | `random` | 每次随机选择一个模板 | 增加多样性 |

**推荐配置**：
- **SINGLE_URL 转载**：`minimal` 或 `academic`（适合单篇精读）
- **GITHUB_TRENDING**：`card` 或 `future`（适合多项目展示）
- **TECH_NEWS**：`modern` 或 `qbit`（适合新闻汇总）

**LaTeX 数学公式支持**：所有模板均支持 LaTeX 数学公式渲染（行内：`$公式$`，块级：`$$公式$$`），由 DoocsMd 渲染器自动处理。

---

## 4. 常见问题 (FAQ)

*   **如何查看 LLM 提取的内容？**
    在 `./data/scraped/` 目录下查找以 `_extracted.md` 结尾的文件。
*   **为什么有些文章被跳过了？**
    系统会自动检查 `./data/registry/url-registry.json`。如果 URL 已存在，说明之前已发布过，系统会跳过以避免重复。
*   **如何强制重新生成已有的文章？**
    在命令中添加参数（如果支持）或者手动清理 `url-registry.json` 中对应的记录。
*   **为什么总结中没有无用信息？**
    我们在 Prompt 中增加了严格的排除约束，要求 LLM 剔除公司高管简介、合作伙伴名单、版权声明等元数据噪音。
