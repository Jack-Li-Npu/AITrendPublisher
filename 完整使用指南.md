# TrendPublish 完整使用指南

> 基于 Deno 开发的趋势发现和内容发布系统，支持多源数据采集、智能总结和自动发布到微信公众号

## 📑 目录

- [快速开始](#快速开始)
- [环境配置](#环境配置)
- [核心功能](#核心功能)
- [工作流系统](#工作流系统)
- [数据源配置](#数据源配置)
- [LLM 大语言模型配置](#llm-大语言模型配置)
- [图片生成配置](#图片生成配置)
- [图片处理](#图片处理)
- [文章模板](#文章模板)
- [通知系统](#通知系统)
- [定时任务](#定时任务)
- [API 接口](#api-接口)
- [本地数据存储](#本地数据存储)
- [部署指南](#部署指南)
- [故障排除](#故障排除)

---

## 🚀 快速开始

### 1. 安装 Deno

**Windows (PowerShell):**
```powershell
irm https://deno.land/install.ps1 | iex
```

**macOS/Linux:**
```bash
curl -fsSL https://deno.land/install.sh | sh
```

**验证安装:**
```bash
deno --version
```

### 2. 克隆项目

```bash
git clone https://github.com/OpenAISpace/ai-trend-publish
cd ai-trend-publish
```

### 3. 配置环境变量

创建 `.env` 文件（参考 `.env.example`）：

```bash
# 最小配置（仅使用 Gemini）
GEMINI_API_KEY="your-gemini-api-key"
GEMINI_MODEL="gemini-2.0-flash-exp"
DEFAULT_LLM_PROVIDER="GEMINI"
AI_SUMMARIZER_LLM_PROVIDER="GEMINI"
AI_CONTENT_RANKER_LLM_PROVIDER="GEMINI"
```

### 4. 运行项目

```bash
# 开发模式（支持热重载）
deno task start

# 测试模式
deno task test
```

---

## ⚙️ 环境配置

### 基础服务配置

#### LLM 服务配置

系统支持多种 LLM 服务提供商，可以根据需要灵活选择和配置：

**支持的提供商对比**

| 提供商 | 类型标识 | 推荐模型 | 成本 | 特点 | API 文档 |
|--------|---------|---------|------|------|---------|
| **OpenAI** | `OPENAI` | gpt-4, gpt-3.5-turbo | 💰💰💰 | 最成熟的商业模型 | [链接](https://platform.openai.com/docs) |
| **DeepSeek** | `DEEPSEEK` | deepseek-chat, deepseek-reasoner | 💰 | 高性价比，推理能力强 | [链接](https://platform.deepseek.com/api-docs) |
| **讯飞星火** | `XUNFEI` | 4.0Ultra | 💰 | 中文理解优秀 | [链接](https://www.xfyun.cn/doc/spark) |
| **通义千问** | `QWEN` | qwen-turbo, qwen-plus, qwen-max | 💰💰 | 阿里云生态 | [链接](https://help.aliyun.com/zh/dashscope/) |
| **Gemini** | `GEMINI` | gemini-2.0-flash-exp, gemini-1.5-pro | 💰💰 | Google 生态，速度快 | [链接](https://ai.google.dev/gemini-api/docs) |
| **自定义** | `CUSTOM` | - | - | OpenAI 兼容 API | - |

**详细配置示例**

**1. Google Gemini API（推荐，速度快）**
```bash
GEMINI_BASE_URL="https://generativelanguage.googleapis.com/v1beta"
GEMINI_API_KEY="your-api-key"
GEMINI_MODEL="gemini-2.0-flash-exp"  # 或 gemini-1.5-pro, gemini-3-flash-preview

# 设置为默认提供商
DEFAULT_LLM_PROVIDER="GEMINI:gemini-2.0-flash-exp"
```

**2. DeepSeek API（推荐，高性价比）**
```bash
DEEPSEEK_BASE_URL="https://api.deepseek.com/v1"
DEEPSEEK_API_KEY="your-api-key"
DEEPSEEK_MODEL="deepseek-chat|deepseek-reasoner|deepseek-coder"  # 支持多模型

# 设置为默认提供商
DEFAULT_LLM_PROVIDER="DEEPSEEK:deepseek-chat"

# 或使用推理模型
DEFAULT_LLM_PROVIDER="DEEPSEEK:deepseek-reasoner"
```

**3. OpenAI API（质量最高）**
```bash
OPENAI_BASE_URL="https://api.openai.com/v1"
OPENAI_API_KEY="your-api-key"
OPENAI_MODEL="gpt-4|gpt-3.5-turbo|gpt-4-turbo"  # 支持多模型

# 设置为默认提供商
DEFAULT_LLM_PROVIDER="OPENAI:gpt-4"
```

**4. 通义千问 API**
```bash
QWEN_BASE_URL="https://dashscope.aliyuncs.com/compatible-mode/v1"
QWEN_API_KEY="your-api-key"
QWEN_MODEL="qwen-turbo|qwen-plus|qwen-max"  # 支持多模型

# 设置为默认提供商
DEFAULT_LLM_PROVIDER="QWEN:qwen-plus"
```

**5. 讯飞星火 API**
```bash
XUNFEI_API_KEY="your-api-key"

# 设置为默认提供商
DEFAULT_LLM_PROVIDER="XUNFEI"

# 注意：讯飞不支持在配置中指定模型，模型由 API 密钥权限决定
```

**6. 自定义 OpenAI 兼容 API**
```bash
CUSTOM_LLM_BASE_URL="https://your-api-endpoint.com/v1"
CUSTOM_LLM_API_KEY="your-api-key"
CUSTOM_LLM_MODEL="your-model-name"

# 设置为默认提供商
DEFAULT_LLM_PROVIDER="CUSTOM:your-model-name"
```

#### 模块功能配置

为不同功能模块指定使用的 LLM 提供者：

```bash
# 默认 LLM 提供者（所有模块的默认选择）
DEFAULT_LLM_PROVIDER="GEMINI:gemini-2.0-flash-exp"  
# 可选: OPENAI | DEEPSEEK | GEMINI | QWEN | XUNFEI | CUSTOM

# 内容排名模块使用的 LLM 提供者
AI_CONTENT_RANKER_LLM_PROVIDER="DEEPSEEK:deepseek-reasoner"  
# 推荐使用 DeepSeek Reasoner，推理能力强

# 内容摘要模块使用的 LLM 提供者
AI_SUMMARIZER_LLM_PROVIDER="GEMINI:gemini-2.0-flash-exp"  
# 推荐使用 Gemini 2.0 Flash，速度快质量好
```

**配置格式说明**

- **基础格式**: `PROVIDER` - 仅指定提供商，使用默认模型
  - 示例: `DEFAULT_LLM_PROVIDER="DEEPSEEK"`
  
- **扩展格式**: `PROVIDER:model` - 指定提供商和具体模型
  - 示例: `DEFAULT_LLM_PROVIDER="DEEPSEEK:deepseek-chat"`
  
- **多模型配置**: 在 `MODEL` 环境变量中用 `|` 分隔多个模型
  - 示例: `OPENAI_MODEL="gpt-4|gpt-3.5-turbo"`
  - 第一个模型为默认模型

#### LLM 选择建议

**按场景选择**

| 场景 | 推荐模型 | 原因 |
|-----|---------|------|
| **内容生成（高质量）** | OpenAI GPT-4 | 最强大的推理和创作能力 |
| **内容生成（性价比）** | DeepSeek Chat, Gemini Flash | 质量好且成本低 |
| **代码生成** | DeepSeek Coder, GPT-4 | 专门优化的代码能力 |
| **中文处理** | 讯飞星火, 通义千问 | 中文理解和生成更自然 |
| **快速响应** | Gemini Flash, GPT-3.5-turbo, Qwen-turbo | 响应速度快 |
| **复杂推理** | DeepSeek Reasoner, GPT-4 | 推理和分析能力强 |

**按成本选择**

| 成本等级 | 推荐模型 | 适用场景 |
|---------|---------|---------|
| 💰 **低成本** | DeepSeek, 讯飞星火, Qwen-turbo | 大量内容生成、测试开发 |
| 💰💰 **中等成本** | Gemini, GPT-3.5-turbo, Qwen-plus | 日常内容生成、生产环境 |
| 💰💰💰 **高质量** | GPT-4, Qwen-max | 重要内容、复杂任务 |

#### 如何获取 API 密钥

| 提供商 | 注册地址 | 说明 |
|--------|---------|------|
| OpenAI | https://platform.openai.com/api-keys | 需要国际信用卡 |
| DeepSeek | https://platform.deepseek.com/ | 支持国内支付 |
| 讯飞星火 | https://www.xfyun.cn/ | 实名认证后免费额度 |
| 通义千问 | https://dashscope.aliyun.com/ | 阿里云账号，免费额度 |
| Gemini | https://ai.google.dev/ | Google 账号，免费额度 |

#### 在代码中使用 LLM

```typescript
import { LLMFactory } from "@src/providers/llm/llm-factory.ts";

// 方式 1: 使用默认提供商
const llm = await LLMFactory.getInstance().getDefaultProvider();

// 方式 2: 指定提供商
const deepseek = await LLMFactory.getInstance().getLLMProvider("DEEPSEEK");

// 方式 3: 指定提供商和模型
const gpt4 = await LLMFactory.getInstance().getLLMProvider("OPENAI:gpt-4");

// 发送消息
const response = await llm.createChatCompletion([
  { role: "system", content: "你是一个有帮助的助手" },
  { role: "user", content: "你好" }
]);

console.log(response.choices[0].message.content);
```

#### 数据抓取配置

**FireCrawl API（网页抓取，支持 v1 和 v2）**
```bash
FIRE_CRAWL_API_KEY="fc-your-api-key"
FIRE_CRAWL_BASE_URL="https://api.firecrawl.dev/v1"  # 可选
```

**Serper API（GitHub Trending 列表抓取）**
```bash
SERPER_API_KEY="your-serper-key"
```

**Jina AI（智能搜索和嵌入）**

```bash
JINA_API_KEY="your-jina-api-key"
```

#### 微信公众号配置

```bash
WEIXIN_APP_ID="your-app-id"
WEIXIN_APP_SECRET="your-app-secret"
AUTHOR="极客AI日记"  # 作者名称
NEED_OPEN_COMMENT=false  # 是否开启评论
ONLY_FANS_CAN_COMMENT=false  # 是否仅粉丝可评论
USE_DOOCS_MD_RENDERER=true  # 使用 DoocsMd 渲染器（推荐）
```

**⚠️ 重要：IP 白名单配置**

在使用微信公众号功能前，需要将服务器 IP 添加到公众号后台的 IP 白名单中：

1. 查看本机 IP: [IP查询工具](https://tool.lu/ip/)
2. 登录微信公众号后台 → 开发 → 基本配置 → IP 白名单
3. 添加服务器 IP 地址

#### 数据库配置（可选）

```bash
ENABLE_DB=true
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_DATABASE=trendfinder
```

#### 通知服务配置

**Bark 通知**
```bash
ENABLE_BARK=true
BARK_URL="your-bark-url"
```

**钉钉通知**
```bash
DINGTALK_WEBHOOK_URL="your-webhook-url"
```

**飞书通知**
```bash
FEISHU_WEBHOOK_URL="your-webhook-url"
```

#### 图片生成配置

```bash
# 图片生成器类型
IMAGE_GENERATOR_TYPE="GEMINI"  # 可选: GEMINI | OPENAI | STABLE_DIFFUSION

# Gemini 图片生成（使用 Gemini 3 Pro）
GEMINI_IMAGE_MODEL="gemini-3-pro-preview"
```

#### 嵌入模型配置

```bash
# 嵌入模型提供者
EMBEDDING_PROVIDER="JINA"  # 可选: JINA | OPENAI

# Jina AI 嵌入模型
JINA_EMBEDDING_MODEL="jina-embeddings-v3"  # 或 jina-embeddings-v2
```

#### 服务器 API 配置

```bash
# JSON-RPC API 认证密钥
SERVER_API_KEY="your-api-key"
```

---

## 🎯 核心功能

### 1. 多源数据采集

#### FireCrawl 网页抓取

支持递归爬取网站内容，可配置每个数据源的递归深度：

- **功能特点**：
  - 智能提取网页主要内容
  - 支持 JavaScript 渲染页面
  - 支持递归爬取内部链接
  - 自动提取标题、内容、发布时间

- **配置示例**：
```typescript
// src/data-sources/getDataSources.ts
{
  identifier: "https://aibusiness.com/latest-news",
  name: "AI Business",
  category: "ai-news",
  maxRecursiveLinks: 1  // 递归爬取 1 篇文章
}
```

#### GitHub Trending 抓取

自动抓取 GitHub 每日/每周热门项目：

```typescript
{
  identifier: "daily",  // 或 "weekly"
  name: "今日热门",
  category: "trending"
}
```

#### Jina AI 智能搜索

使用 Jina AI 的 DeepSearch 功能进行智能内容搜索和提取。

### 2. AI 智能处理

#### 内容总结与摘要

- 使用配置的 LLM 模型（默认：Gemini）对文章进行智能总结
- 提取关键信息和要点
- 生成适合微信公众号的摘要格式

#### 标题生成

- AI 自动生成吸引人的标题
- 限制长度：**13 个字符以内**（符合微信标题限制）
- 支持中英文混合标题

#### 内容质量评分与排序

- 使用 AI 对文章进行质量评分
- 根据评分自动排序，优先处理高质量内容
- 可配置使用不同的模型（推荐：DeepSeek Reasoner）

#### 内容去重

- 使用向量嵌入模型计算文章相似度
- 自动识别并过滤重复内容
- 支持配置相似度阈值

#### 向量嵌入与语义检索

- 使用 Jina AI 或 OpenAI 嵌入模型
- 将文章转换为向量表示
- 支持语义相似度计算和检索

#### 智能缓存机制

- LLM 响应缓存，减少 API 调用成本
- 自动识别重复请求
- 可配置缓存策略

### 3. 图片处理

#### AI 图片生成

- **Gemini 3 Pro 图片生成**：
  - 支持生成文章封面图片
  - 支持生成整体介绍图片（1K 分辨率：1024x576）
  - 自动上传到微信 CDN

- **图片生成流程**：
  1. 根据文章内容生成图片提示词
  2. 调用 Gemini 3 Pro 生成图片（base64）
  3. 自动上传到微信 CDN
  4. 替换为 CDN URL 插入文章

#### 图片压缩与格式转换

- 自动压缩超过 1MB 的图片
- 格式转换（WebP → JPEG）
- 优化图片质量，确保微信兼容性

#### 自动图片填充

- 为没有图片的文章自动生成封面图
- 智能识别文章主题
- 生成符合文章风格的图片

### 4. 文章渲染

#### Doocs MD 渲染器（推荐）

- 基于 `@doocs/md` 的 Markdown 渲染器
- 微信兼容性优秀
- 支持代码高亮、表格、列表等
- 自动处理图片、链接等元素

#### 传统 EJS 模板渲染器

- 支持多种自定义模板
- 灵活的内容布局
- 可自定义样式

#### Markdown 处理

- 自动处理 Markdown 语法
- 标题层级规范化（`##` → `###`）
- 处理加粗、斜体等格式
- 代码块高亮

### 5. 微信公众号发布

#### 文章发布

- 自动上传图片到微信 CDN
- 发布文章到微信公众号
- 支持草稿箱和正式发布
- 自动处理文章格式和样式

#### 内容限制处理

- 标题长度限制：64 字节（约 13 个中文字符）
- 内容大小限制：自动检测并优化
- 图片大小限制：自动压缩和转换

---

## 🔄 工作流系统

### 工作流类型

系统提供三种主要工作流：

#### 1. WeixinArticleWorkflow（科技新闻文章）

**功能**：
- 从多个科技新闻网站抓取内容
- AI 总结和生成文章
- 发布到微信公众号

**触发方式**：
- 定时任务（每天凌晨 3 点）
- JSON-RPC API 手动触发

**参数配置**：
```typescript
{
  sourceType?: "all" | "firecrawl" | "twitter" | "github",  // 数据源类型
  contentMode?: "TECH_NEWS" | "GITHUB_TRENDING",  // 内容模式
  maxArticles?: number,  // 最大文章数（默认 4）
  forcePublish?: boolean,  // 强制发布
  template?: "default" | "modern" | "tech" | "mianpro" | "qbit"  // 模板类型
}
```

#### 2. WeixinAIBenchWorkflow（AI Bench 排行榜）

**功能**：
- 抓取 AI 模型性能排行榜数据
- 生成排行榜文章
- 发布到微信公众号

#### 3. WeixinHelloGithubWorkflow（Hello GitHub 推荐）

**功能**：
- 抓取 Hello GitHub 项目推荐
- 生成项目推荐文章
- 发布到微信公众号

### 工作流执行流程

```
1. 验证 IP 白名单
   ↓
2. 获取数据源配置
   ↓
3. 抓取内容（FireCrawl/Twitter/GitHub）
   ↓
4. 保存到本地存储（按源分类）
   ↓
5. 从本地存储随机加载文章
   ↓
6. 内容去重（向量相似度）
   ↓
7. 内容质量评分与排序
   ↓
8. AI 总结和生成文章
   ↓
9. 生成文章标题
   ↓
10. 生成整体介绍图片和文本
    ↓
11. 生成文章封面图片
    ↓
12. 渲染文章（Markdown → HTML）
    ↓
13. 上传图片到微信 CDN
    ↓
14. 发布到微信公众号
    ↓
15. 发送通知
```

---

## 📊 数据源配置

### 配置文件位置

`src/data-sources/getDataSources.ts`

### 科技新闻模式（TECH_NEWS）

默认数据源配置：

```typescript
export const techNewsSourceConfigs: SourceConfig = {
  firecrawl: [
    {
      identifier: "https://aibusiness.com/latest-news",
      name: "AI Business",
      category: "ai-news",
      maxRecursiveLinks: 1  // 每个源爬取 1 篇文章
    },
    {
      identifier: "https://www.therobotreport.com/category/news/",
      name: "The Robot Report",
      category: "robotics-news",
      maxRecursiveLinks: 1
    },
    {
      identifier: "https://aimagazine.com/top10",
      name: "AI Magazine",
      category: "ai-news",
      maxRecursiveLinks: 1
    },
    {
      identifier: "https://news.mit.edu/topic/artificial-intelligence2",
      name: "MIT News AI",
      category: "ai-news",
      maxRecursiveLinks: 1
    }
  ],
  twitter: [],  // 默认禁用（需要付费 API）
  github: []
};
```

### GitHub Trending 模式（GITHUB_TRENDING）

```typescript
export const gitHubSourceConfigs: SourceConfig = {
  firecrawl: [],
  twitter: [],
  github: [
    { identifier: "daily", name: "今日热门", category: "trending" },
    { identifier: "weekly", name: "本周热门", category: "trending" }
  ]
};
```

**工作逻辑详解**：
1. **列表抓取**：调用 **Serper API** 获取 GitHub Trending 页面的纯文本列表。
2. **项目解析**：自动解析 `owner / repo` 格式的项目全名，并拼接为 GitHub 详情 URL。
3. **详情提取**：调用 **FireCrawl v2 API** 抓取项目 README 或详情页的 Markdown（支持 `maxAge` 缓存）。
4. **深度分析**：采用专家级 Prompt 生成包含“架构解析”、“实战指南”、“性能对比表格”的公众号深度文章。
5. **视频链接**：自动检测文档中的 `[Video Link](url)` 并提取，供后续文章插入。

### 添加自定义数据源

#### 方式一：修改代码配置

编辑 `src/data-sources/getDataSources.ts`，在对应数组中添加：

```typescript
firecrawl: [
  {
    identifier: "https://your-website.com",
    name: "Your Source Name",
    category: "custom",
    maxRecursiveLinks: 3  // 递归爬取 3 篇文章
  }
]
```

#### 方式二：使用数据库（需要启用数据库）

在数据库的 `data_sources` 表中添加记录：

```sql
INSERT INTO data_sources (identifier, platform) 
VALUES ('https://your-website.com', 'firecrawl');
```

### 切换内容模式

在 `.env` 文件中配置：

```bash
CONTENT_MODE="TECH_NEWS"  # 或 "GITHUB_TRENDING"
```

---

## 🤖 AI 模型配置

### 支持的模型

#### Gemini 系列
- `gemini-3-flash-preview` - 最新预览版，速度快
- `gemini-2.0-flash-exp` - 实验版，性能优秀
- `gemini-1.5-pro` - 适合复杂任务
- `gemini-1.5-flash` - 平衡速度和质量

#### DeepSeek 系列
- `deepseek-chat` - 通用对话模型
- `deepseek-reasoner` - 推理能力强，适合内容排名

#### OpenAI 系列
- `gpt-4` - 最强性能
- `gpt-3.5-turbo` - 性价比高

### 多模型配置

支持为不同模块配置不同的模型：

```bash
# 摘要模块使用 Gemini（速度快，质量好）
AI_SUMMARIZER_LLM_PROVIDER="GEMINI"
GEMINI_MODEL="gemini-2.0-flash-exp"

# 内容排名使用 DeepSeek Reasoner（推理能力强）
AI_CONTENT_RANKER_LLM_PROVIDER="DEEPSEEK:deepseek-reasoner"
```

### 模型切换策略

1. **成本优化**：使用 Gemini（免费额度高）或 DeepSeek（价格低）
2. **质量优先**：使用 GPT-4 或 Gemini 1.5 Pro
3. **速度优先**：使用 Gemini Flash 或 DeepSeek Chat
4. **混合策略**：摘要用 Gemini，排名用 DeepSeek Reasoner

---

## 🖼️ 图片处理

### 图片生成

#### 文章封面图片

- **生成时机**：文章内容生成后
- **分辨率**：自动优化
- **上传**：自动上传到微信 CDN

#### 整体介绍图片

- **生成时机**：所有文章生成后
- **分辨率**：1K（1024x576）
- **用途**：文章开头整体介绍
- **上传**：自动上传到微信 CDN，替换 base64

### 图片处理流程

```
1. 生成图片（base64）
   ↓
2. 验证图片格式和大小
   ↓
3. 压缩（如果超过 1MB）
   ↓
4. 格式转换（WebP → JPEG）
   ↓
5. 上传到微信 CDN
   ↓
6. 替换为 CDN URL
```

### 图片配置

```bash
# 图片生成器类型
IMAGE_GENERATOR_TYPE="GEMINI"  # 使用 Gemini 3 Pro

# Gemini 图片模型
GEMINI_IMAGE_MODEL="gemini-3-pro-preview"
```

---

## 📝 文章模板

### 可用模板

系统提供 5 种文章模板：

1. **default** - 默认模板，简洁大方
2. **modern** - 现代风格，时尚美观
3. **tech** - 科技风格，专业感强
4. **mianpro** - 专业风格，适合深度内容
5. **qbit** - 量子位/机器之心风格，专业科技

### 模板配置

#### 方式一：环境变量配置

```bash
WEIXIN_ARTICLE_TEMPLATE_TYPE="modern"  # 指定模板
# 或
WEIXIN_ARTICLE_TEMPLATE_TYPE="random"  # 随机选择
```

#### 方式二：API 参数指定

```json
{
  "method": "triggerWorkflow",
  "params": {
    "workflowType": "weixin-article-workflow",
    "template": "tech"
  }
}
```

### 模板预览

查看 [模板展示页面](https://openaispace.github.io/ai-trend-publish/templates.html) 了解各模板效果。

---

## 🔔 通知系统

### Bark 通知

**配置**：
```bash
ENABLE_BARK=true
BARK_URL="https://api.day.app/your-key"
```

**功能**：
- 工作流开始/完成通知
- 错误告警
- 定时任务启动通知

### 钉钉通知

**配置**：
```bash
DINGTALK_WEBHOOK_URL="https://oapi.dingtalk.com/robot/send?access_token=xxx"
```

**功能**：
- 工作流执行状态
- 错误告警
- 自定义消息推送

### 飞书通知

**配置**：
```bash
FEISHU_WEBHOOK_URL="https://open.feishu.cn/open-apis/bot/v2/hook/xxx"
```

**功能**：
- 工作流执行状态
- 错误告警
- 自定义消息推送

---

## ⏰ 定时任务

### 定时任务配置

系统在每天凌晨 3 点（Asia/Shanghai 时区）自动执行工作流。

### 按星期配置工作流

在 `.env` 文件中配置每天使用的工作流：

```bash
# 周一使用的工作流
1_of_week_workflow="weixin-article-workflow"

# 周二使用的工作流
2_of_week_workflow="weixin-aibench-workflow"

# 周三使用的工作流
3_of_week_workflow="weixin-hellogithub-workflow"

# ... 以此类推
```

**可用工作流类型**：
- `weixin-article-workflow` - 科技新闻文章
- `weixin-aibench-workflow` - AI Bench 排行榜
- `weixin-hellogithub-workflow` - Hello GitHub 推荐

### 修改定时任务时间

编辑 `src/controllers/cron.ts`：

```typescript
// 每天凌晨 3 点执行
cron.schedule("0 3 * * *", async () => {
  // 修改为其他时间，例如每天上午 10 点
  // cron.schedule("0 10 * * *", async () => {
```

---

## 🔌 API 接口

### JSON-RPC API

系统提供基于 JSON-RPC 2.0 协议的 API，支持手动触发工作流。

#### 端点

POST http://localhost:8000/api/workflow
```

#### 认证

需要在请求头中添加：

```
Authorization: Bearer your-api-key
```

在 `.env` 中配置：

```bash
SERVER_API_KEY="your-api-key"
```

#### 触发工作流

**请求示例**：

```json
{
  "jsonrpc": "2.0",
  "method": "triggerWorkflow",
  "params": {
    "workflowType": "weixin-article-workflow",
    "sourceType": "all",
    "contentMode": "TECH_NEWS",
    "maxArticles": 4,
    "template": "modern",
    "forcePublish": false
  },
  "id": "1"
}
```

**响应示例**：

```json
{
  "jsonrpc": "2.0",
  "result": {
    "success": true,
    "workflowId": "weixin-article-workflow",
    "eventId": "cron-job"
  },
  "id": "1"
}
```

#### 参数说明

| 参数 | 类型 | 说明 | 默认值 |
|------|------|------|--------|
| workflowType | string | 工作流类型 | 必填 |
| sourceType | string | 数据源类型（all/firecrawl/twitter/github） | all |
| contentMode | string | 内容模式（TECH_NEWS/GITHUB_TRENDING） | TECH_NEWS |
| maxArticles | number | 最大文章数 | 4 |
| template | string | 模板类型 | default |
| forcePublish | boolean | 强制发布 | false |

**详细文档**：[JSON-RPC API 文档](https://openaispace.github.io/ai-trend-publish/json-rpc-api.html)

---

## 💾 本地数据存储

### 存储结构

抓取的数据按以下结构存储：

```
data/scraped/
  └── YYYY-MM-DD-HHMMSS/  # 时间戳文件夹
      ├── aibusiness_com/
      │   ├── scraped-data.json  # 抓取的文章数据
      │   └── metadata.json      # 元数据
      ├── aimagazine_com/
      │   ├── scraped-data.json
      │   └── metadata.json
      └── ...
```

### 数据格式

`scraped-data.json` 包含文章数组：

```json
[
  {
    "id": "article-id",
    "title": "文章标题",
    "content": "文章内容",
    "url": "https://example.com/article",
    "publishDate": "2026-01-09",
    "media": [],
    "metadata": {}
  }
]
```

### 使用方式

系统自动：
1. **保存**：抓取完成后自动保存到本地
2. **加载**：从最新时间戳文件夹随机加载文章
3. **选择**：从每个数据源随机选择指定数量的文章

### 手动管理

可以手动查看和管理 `data/scraped/` 目录下的数据。

---

## 🚀 部署指南

### 方式一：直接部署

#### 1. 安装 Deno

```bash
# Windows
irm https://deno.land/install.ps1 | iex

# Linux/macOS
curl -fsSL https://deno.land/install.sh | sh
```

#### 2. 克隆项目

```bash
git clone https://github.com/OpenAISpace/ai-trend-publish.git
cd ai-trend-publish
```

#### 3. 配置环境变量

```bash
cp .env.example .env
# 编辑 .env 文件
```

#### 4. 启动服务

```bash
# 开发模式
deno task start

# 使用 PM2 管理（推荐）
npm install -g pm2
pm2 start --interpreter="deno" --interpreter-args="run --allow-all --env-file=.env" src/index.ts

# 设置开机自启
pm2 startup
pm2 save
```

### 方式二：Docker 部署

#### 1. 构建镜像

```bash
docker build -t ai-trend-publish .
```

#### 2. 运行容器

```bash
# 使用环境变量文件
docker run -d --env-file .env --name ai-trend-publish-container ai-trend-publish

# 或直接指定环境变量
docker run -d \
  -e GEMINI_API_KEY="your-key" \
  -e WEIXIN_APP_ID="your-id" \
  --name ai-trend-publish-container \
  ai-trend-publish
```

### 方式三：编译为可执行文件

```bash
# Windows
deno task build:win

# macOS (Intel)
deno task build:mac-x64

# macOS (Apple Silicon)
deno task build:mac-arm64

# Linux (x64)
deno task build:linux-x64

# Linux (ARM64)
deno task build:linux-arm64

# 编译所有平台
deno task build:all
```

编译后的可执行文件位于项目根目录。

---

## 🔧 故障排除

### 常见问题

#### 1. Gemini API Key 无效

**错误信息**：
```
生成计划失败: Gemini API 错误: API key not valid. Please pass a valid API key.
```

**解决方法**：
1. 检查 `.env` 文件中的 `GEMINI_API_KEY` 是否正确
2. 获取新的 API Key: https://aistudio.google.com/
3. 确保 API Key 未过期或被撤销

#### 2. IP 白名单验证失败

**错误信息**：
```
当前服务器IP(xxx.xxx.xxx.xxx)不在微信公众号IP白名单中
```

**解决方法**：
1. 查看本机 IP: [IP查询工具](https://tool.lu/ip/)
2. 登录微信公众号后台
3. 开发 → 基本配置 → IP 白名单
4. 添加服务器 IP 地址

#### 3. 内容大小超出限制

**错误信息**：
```
content size out of limit
```

**解决方法**：
1. 减少文章数量（`maxArticles`）
2. 检查图片是否已上传到 CDN（不应使用 base64）
3. 优化文章内容长度

#### 4. 标题长度超出限制

**错误信息**：
```
title size out of limit
```

**解决方法**：
- 系统已自动限制标题为 13 个字符以内
- 如果仍出现错误，检查是否有特殊字符占用额外字节

#### 5. 图片上传失败

**可能原因**：
- 微信 API 认证失败
- 图片格式不支持
- 图片大小超出限制

**解决方法**：
1. 检查微信 AppID 和 AppSecret 是否正确
2. 确保图片格式为 JPEG 或 PNG
3. 检查图片大小（系统会自动压缩）

#### 6. 数据源抓取失败

**可能原因**：
- FireCrawl API Key 无效
- 网站结构变化
- 网络连接问题

**解决方法**：
1. 检查 `FIRE_CRAWL_API_KEY` 是否正确
2. 查看 FireCrawl 账户额度
3. 检查目标网站是否可访问

#### 7. LLM API 调用失败

**可能原因**：
- API Key 无效或过期
- API 额度用尽
- 网络连接问题

**解决方法**：
1. 检查对应 API Key 配置
2. 查看 API 账户余额和额度
3. 检查网络连接
4. 尝试切换到其他 LLM 提供者

### 日志查看

系统使用 `@zilla/logger` 进行日志记录，日志级别可在代码中配置：

```typescript
Logger.level = LogLevel.INFO;  // DEBUG | INFO | WARN | ERROR
```

### 调试模式

运行测试模式查看详细输出：

```bash
deno task test
```

---

## 📚 相关文档

- [快速开始指南](docs/quick-start-guide.md)
- [环境配置说明](ENV_CONFIGURATION.md)
- [数据源配置指南](docs/data-sources-guide.md)
- [Gemini 集成指南](docs/gemini-integration-guide.md)
- [Jina AI 集成指南](docs/jina_integration_guide.md)
- [JSON-RPC API 文档](docs/json-rpc-api.md)
- [Doocs MD 渲染器指南](docs/doocs-md-renderer-guide.md)
- [钉钉 Webhook 指南](docs/dingtalk-webhook-guide.md)

---

## 🤝 获取帮助

- **GitHub Issues**: [提交问题](https://github.com/OpenAISpace/ai-trend-publish/issues)
- **QQ 群**: [TrendPublish-2](https://qm.qq.com/q/ZSOmGi01S8)
- **示例公众号**: **AISPACE科技空间**

---

## 📄 许可证

本项目采用 MIT 许可证 - 详见 [LICENSE](LICENSE) 文件

---

**最后更新**: 2026-01-09
